# Minimal Jenkins for simple demos. Behind a TLS-inspecting proxy the apt step
# may fail with a certificate error — use Dockerfile_Master (opt-in certs/) instead.
FROM jenkins/jenkins:latest

USER root

RUN apt-get update && apt-get install -y \
    sudo \
    curl \
    git \
    && rm -rf /var/lib/apt/lists/*

USER jenkins
